# Supabase connection — loaded by Vite in every mode (dev + build).
# These are PUBLIC client credentials: the publishable key ships in the JS
# bundle by design, and data access is protected by RLS + SECURITY DEFINER
# RPCs, not by key secrecy. That's why this file is committed to git.
# Values come from the Supabase dashboard: Project Settings -> Data API.

VITE_SUPABASE_URL=https://plsugxnukitbuxbtfgow.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_NTUuo10SQYyyKVT3g52hTg_g3uDmKyS
